
#-----------------------------------------------------------------------------
# Version file for install directory
#-----------------------------------------------------------------------------
set(PACKAGE_VERSION @TAILORFS_PACKAGE_VERSION@)

if ("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @TAILORFS_VERSION_MAJOR@)
    set(PACKAGE_VERSION_COMPATIBLE 1)
    if ("${PACKAGE_FIND_VERSION_PATCH}" EQUAL @TAILORFS_VERSION_PATCH@)
        set(PACKAGE_VERSION_EXACT 1)
    endif ()
endif ()
